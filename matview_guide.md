# Matview 가이드

Redash 쿼리에 matview(증분 캐시)를 적용할 수 있는지 판정하고, 실제로 쿼리를 변환하는 방법.
LLM에게 그대로 물리는 압축본은 [`matview_manual.md`](matview_manual.md)이고, **내용은 같다.** 이 문서는 서술만 풀어 쓴 것이다.
설계 배경과 구현 내부는 [`.plans/ticketed/ML-5958-matview.md`](.plans/ticketed/ML-5958-matview.md), 티켓은 [ML-5958](https://teamdable.atlassian.net/browse/ML-5958).

## 목차

- [용어](#용어)
- [1. 동작 방식](#1-동작-방식)
- [2. 적용할 수 있는 쿼리인지 판정하기](#2-적용할-수-있는-쿼리인지-판정하기)
- [3. 쿼리 변환하기](#3-쿼리-변환하기)
- [4. "지금 상태"를 참조하는 조인 처리하기](#4-지금-상태를-참조하는-조인-처리하기)
- [5. 결과가 시간별 행이 아닐 때: 원본 스캔만 캐시하기](#5-결과가-시간별-행이-아닐-때-원본-스캔만-캐시하기)
- [6. 함정 체크리스트](#6-함정-체크리스트)
- [7. 분석과 변환 결과를 적는 형식](#7-분석과-변환-결과를-적는-형식)

---

## 용어

이 문서에서 반복해서 쓰는 말들이다. 처음 읽는다면 여기부터 본다.

| 용어 | 뜻 |
|---|---|
| **버킷(bucket)** | 결과의 한 행이 대표하는 시간 단위. `day` 또는 `hour` 둘만 지원한다. 예를 들어 "일자별 매출" 쿼리의 버킷은 `day`다. |
| **버킷 컬럼(bucket_col)** | 그 행이 어느 버킷에 속하는지를 담고 있는 결과 컬럼. 예: `utc_basic_time`. |
| **어노테이션(annotation)** | 쿼리 맨 위에 넣는 `-- matview: ...` 주석 한 줄. 이 줄이 있어야 matview가 동작한다. |
| **마커(marker)** | 시간 하한 필터에 심어 두는 `/*matview:day*/'1970-01-01'` 같은 자리표시자. 스케줄 실행 직전에 재계산 시작 시각으로 치환된다. |
| **`matview_start`** | 이번 실행에서 다시 계산할 구간의 시작 시각. 마커가 이 값으로 치환된다. |
| **full 재적재** | 증분이 아니라 조회 기간 전체를 처음부터 다시 실행하는 것. |
| **박제** | 재계산 범위 밖으로 밀려난 과거 버킷이, 마지막으로 계산됐던 시점의 값 그대로 굳어 버리는 것. 이 문서에서만 쓰는 말이다. |

---

## 1. 동작 방식

### 1.1 전제 조건

- **스케줄이 등록된 쿼리에서만 동작한다.** 스케줄이 없는 쿼리는 증분 경로를 타지 않고 항상 전 기간으로 실행된다.
- 서버에서 `REDASH_FEATURE_MATVIEW`가 켜져 있어야 한다(기본값 `true`).

### 1.2 매 스케줄 실행이 하는 일

먼저 다시 계산할 구간의 시작 시각을 정한다.

```
matview_start = 버킷 경계로 내림( min(지금, 직전 결과 시각) − refresh )
```

그다음 세 단계로 결과를 만든다.

1. 쿼리를 `matview_start` **이후 범위로만** 실행해 새 행을 얻는다.
2. 직전 실행 결과에서 `[지금 − retention, matview_start)` 구간의 행을 그대로 가져온다.
3. 둘을 이어붙여 이번 실행의 결과로 저장한다.

기준이 `지금`이 아니라 `min(지금, 직전 결과 시각)`이라는 점이 중요하다. 실행이 밀리거나 몇 번 실패해도 재계산 구간이 그만큼 자동으로 넓어져서, 빠진 구간을 스스로 메운다.

### 1.3 full 재적재가 걸리는 경우

| 조건 | 설명 |
|---|---|
| 첫 실행 | 재사용할 직전 결과가 없다 |
| SQL 본문 변경 | 주석과 공백을 제외한 해시가 기준이다 |
| 어노테이션 문자열 변경 | 뒤에 나오는 `v` 값을 바꾸는 것도 여기 해당한다 |
| 결과 컬럼 집합 변경 | 그 실행은 새 행만 저장하고, 실제 full 재적재는 **다음 실행**에서 일어난다 |
| 쿼리 페이지의 Refresh 버튼 | 스케줄 실행이 아니라서 마커가 치환되지 않고, 원래 하한이 살아나 전 기간으로 돌아간다 |

### 1.4 과거 버킷은 박제된다

**`matview_start` 이전 버킷은 다시 계산되지 않는다.** 그 버킷이 마지막으로 계산됐을 때의 원본 데이터와 조인 결과가 그대로 굳는다. 이후 원본이 수정되거나 조인 대상 테이블의 상태가 바뀌어도 반영되지 않는다.

판정 작업의 대부분은 결국 이 질문 하나로 수렴한다.

> **이 박제가 쿼리의 의미를 깨는가?**

---

## 2. 적용할 수 있는 쿼리인지 판정하기

### 2.1 버킷 컬럼 찾기

**모든 출력 행이 정확히 하나의 시간 버킷에 귀속되는 컬럼**을 찾는다. 행 하나가 여러 시간대에 걸쳐 있으면 안 된다.

값의 형식은 아래 셋만 지원한다.

- ISO datetime
- `'yyyy-MM-dd'`
- `'yyyy-MM-dd-HH'`

해당하는 컬럼이 없으면 부적합이다. 다만 **결과가 전 기간 요약이라서 버킷이 없을 뿐이고, 비용의 정체는 원본 재스캔**인 경우가 있다. 그때는 [5장](#5-결과가-시간별-행이-아닐-때-원본-스캔만-캐시하기)으로 간다.

### 2.2 버킷 단위로 쪼갤 수 있는지 확인하기

박제를 깨는 구조를 쿼리 안에서 전부 찾아 표시한다.

| 발견한 구조 | 왜 문제인가 | 대응 |
|---|---|---|
| 전 기간 집계(GROUP BY에 버킷이 없다), 버킷 경계를 넘는 윈도우 함수 | 한 행의 값이 여러 버킷의 데이터로 결정되므로, 최근 구간만 다시 계산하면 값이 틀어진다 | 버킷 단위를 키우면 해소되는지 확인한다 |
| 현재 상태를 읽는 조인이나 필터(`__latest` 테이블 등) | 과거 버킷이 "그때의 상태"로 박제된다 | [4장](#4-지금-상태를-참조하는-조인-처리하기)으로 간다 |
| 롤링 윈도우 기준 필터(최근 N일 실적으로 대상을 선정하는 등) | 대상 목록 자체가 실행 시점마다 달라지는데, 과거 버킷은 옛날 목록으로 굳는다 | 박제를 감수하거나, "그 시각 기준"으로 다시 쓴다 |
| 미래 데이터로 값이 확정되는 지표 | 버킷 H의 값이 H 이후에 들어온 데이터로 결정된다(attribution 등). 확정되기 전에 박제되면 값이 영구히 미달된다 | `refresh`를 확정 지연만큼 키워서 흡수한다 |

> **버킷 단위를 키운다는 게 무슨 뜻인가**
> 하루 단위로 `PARTITION BY date(...)` 하는 윈도우 함수가 있다고 하자. `bucket=day`로 선언하면 재계산 단위가 하루 통째라 윈도우가 항상 온전히 들어와 안전하다.
> 같은 쿼리를 `bucket=hour`로 선언하면, 하루의 앞부분 시간대가 아직 그날 데이터가 다 안 들어온 상태에서 계산되고 그대로 박제된다.

### 2.3 결론 내기

셋 중 하나로 결론을 내고, 근거를 함께 쓴다.

- **적합**
- **조건부** — 무엇을 바꿔야 하는지 함께 적는다
- **부적합** — 이유를 함께 적는다

---

## 3. 쿼리 변환하기

### 3.1 어노테이션 붙이기

쿼리 최상단에 주석 한 줄을 넣는다.

```sql
-- matview: bucket_col=<컬럼> bucket=<day|hour> retention=<N>d refresh=<N>h v=1
```

| 필드 | 무엇을 넣나 |
|---|---|
| `bucket_col` | 2.1에서 찾은 버킷 컬럼 이름 |
| `bucket` | `day` 또는 `hour` |
| `retention` | 현재 쿼리가 조회하는 기간 그대로 |
| `refresh` | 매 실행마다 다시 계산할 최근 구간의 길이. 아래 공식으로 정한다 |
| `v` | 아무 값이나 넣는 자유 필드. 값을 바꾸면 강제로 full 재적재가 걸리는 레버로 쓴다 |

**`refresh` 최소값 = 버킷 값이 최종 확정되기까지의 지연 + 스케줄 간격**

이 값보다 짧게 잡으면, 재사용하는 과거 행과 새로 계산한 행 사이에 **영구적인 구멍**이 생긴다. 한 번 생기면 full 재적재를 걸기 전까지 사라지지 않는다.

| 상황 | refresh |
|---|---|
| 원본이 3시간 뒤에 확정되고, 쿼리는 매시간 실행 | `4h` |
| 전환이 하루쯤 소급 집계되는 지표 | `28h` |
| 앞으로 24시간을 보는 attribution 윈도우가 있고, 쿼리는 하루 한 번 실행 | `48h` |

### 3.2 마커 심기

**fact 테이블**(시간 파티션 필터가 걸려 있는 큰 테이블)의 **시간 하한에만** 마커를 심는다.

```sql
-- 변경 전
where utc_basic_time >= format_datetime(current_timestamp - interval '60' day, 'yyyy-MM-dd')

-- 변경 후
where utc_basic_time >= greatest(/*matview:day*/'1970-01-01',
      format_datetime(current_timestamp - interval '60' day, 'yyyy-MM-dd'))
```

지켜야 할 것.

- 파티션 값의 형식이 `'yyyy-MM-dd-HH'`면 `/*matview:hour*/'1970-01-01-00'`을 쓴다.
- **원래 하한을 반드시 `greatest()` 안에 남겨 둔다.** 에디터에서 직접 실행하거나 Refresh 버튼을 누르면 마커가 치환되지 않고 `'1970-01-01'`이 그대로 들어가는데, `greatest()`가 있으면 원래 하한이 선택되어 전 기간 조회로 정상 동작한다. 마커만 남기고 원래 하한을 지우면 그 경로에서 1970년부터 전부 스캔한다.
- **상한과 JOIN 조건은 손대지 않는다.**
- 파티션 시각이 버킷 시각보다 앞설 수 있는 테이블(예: 이벤트 발생 시각이 아니라 적재 시각으로 파티셔닝된 테이블)은 마커 값에 여유를 준다.

  ```sql
  date_add('day', -1, date_parse(/*matview:hour*/'1970-01-01-00', '%Y-%m-%d-%H'))
  ```

### 3.3 마커를 심지 않는 테이블

상태 테이블과 디멘전 테이블(`__latest`, 컨트롤/메타 테이블, 인라인 `VALUES`)에는 마커를 심지 않는다.

두 가지 이유다. 크기가 작아서 절감 효과가 없고, 최근 버킷을 계산할 때도 `matview_start` 이전 시점의 상태가 필요할 수 있다. 매번 전체(또는 retention에 여유를 더한 범위)를 스캔하도록 그냥 둔다.

### 3.4 파라미터 문법 금지

`{{ }}` 파라미터 문법은 쓰지 않는다. Redash 파라미터 시스템과 충돌한다.

---

## 4. "지금 상태"를 참조하는 조인 처리하기

일부 쿼리는 버킷 시각과 무관하게 **"지금 이 순간의 상태"** 를 읽어서 쓴다. 예를 들어 `__latest` 테이블에서 광고주의 현재 활성 여부를 가져와 필터로 쓰거나, 현재 캠페인 이름을 붙이는 경우다.

matview에서는 이게 문제가 된다. 3개월 전 버킷이 재계산될 때 참조한 것은 **그때의 현재 상태**이고, 그 값이 그대로 박제되기 때문이다. 그 뒤에 광고주가 비활성으로 바뀌어도 과거 버킷은 계속 활성으로 남는다.

이런 조인과 필터를 하나씩 짚어서, 아래 세 가지 중 하나를 고른다.

### 선택지 A. 박제를 그냥 받아들인다

**언제**: 참조하는 속성이 사실상 변하지 않거나(생성될 때 고정되는 매핑, 디멘전 이름, OS 등), 오히려 "그 시점 기준 값"으로 남는 게 원래 의도에 맞을 때.

**할 일**: 쿼리는 그대로 두되, 판정 결과에 이 선택을 명시한다. 그리고 **full 재적재가 일어날 때는 현재 상태가 전 기간에 소급 적용된다**는 점도 함께 적는다. 즉 이 쿼리의 과거 값은 full 재적재 전후로 달라질 수 있다.

### 선택지 B. "그 시각에 유효했던 값"으로 조인을 다시 쓴다

**언제**: 과거 행의 값이나 포함 여부가 현재 상태로 결정되면 안 되고, 상태 변경 이력이 남는 테이블이 있을 때.

**할 일**: "가장 최근 값 하나를 고르는" 조인을, "각 값이 유효했던 시간 구간을 만들어서 버킷 시각으로 맞춰 붙이는" 조인으로 바꾼다.

```sql
ctrl as (  -- (client, 시각) 조합에 스냅샷이 여러 개면 먼저 하나로 접는다
    select client_id, utc_basic_time, max(value) as value
    from history_table
    where utc_basic_time >= format_datetime(current_timestamp - interval '<retention+1>' day, 'yyyy-MM-dd-HH')
    group by 1, 2
),
ctrl_range as (  -- 다음 스냅샷 시각을 끌어와 [valid_from, valid_to) 구간을 만든다
    select *, utc_basic_time as valid_from,
           lead(utc_basic_time) over (partition by client_id order by utc_basic_time) as valid_to
    from ctrl
)
-- JOIN: a.utc_basic_time >= d.valid_from and (d.valid_to is null or a.utc_basic_time < d.valid_to)
```

이력 테이블은 `retention + 1일`까지 스캔한다. 첫 버킷을 계산하려면 그 직전 상태가 필요하기 때문에 하루를 더 본다. 이 테이블에는 마커를 심지 않는다(3.3 참고).

### 선택지 C. matview를 포기한다

**언제**: 이력 테이블이 없는데 박제도 허용할 수 없을 때.

**할 일**: matview 적용을 포기하거나, 상태 필터를 쿼리에서 빼고 **결과 컬럼으로 내려서** 시각화 단계에서 거르도록 제안한다.

---

## 5. 결과가 시간별 행이 아닐 때: 원본 스캔만 캐시하기

### 5.1 어떤 상황인가

2.1에서 버킷 컬럼을 못 찾는 경우가 있다. 결과가 시간별 행이 아니라 전 기간 요약(숫자 카드 몇 줄 같은)이고, 그 요약이 전 구간 `count_distinct`처럼 **부분 결과를 더해서는 전체를 만들 수 없는 집계**를 품고 있을 때다. 이러면 결과 자체는 matview 대상이 될 수 없다.

그런데 비용의 정체를 뜯어 보면, 실제로 비싼 건 집계가 아니라 **큰 원본 로그(특히 raw JSON blob)를 매번 전 구간 다시 스캔하는 것**인 경우가 많다.

그렇다면 캐시 경계를 최종 결과가 아니라 **파싱이 끝난 직후의 행 단위 층으로 내리면** 된다.

### 5.2 쿼리를 base와 reader로 나눈다

| 쿼리 | 하는 일 |
|---|---|
| **base** (여기에 matview를 적용한다) | 원본을 스캔해서 blob은 버리고 **필요한 파싱 결과 스칼라만** 남긴 행 단위 결과를 낸다(정보 손실이 없다면 GROUP BY로 접어도 된다). 버킷 컬럼(파티션 시각)을 SELECT에 포함시킨다. 각 행이 하나의 버킷에 속하고 로그 행은 한 번 쓰이면 변하지 않으므로 matview에 적합하다. 어노테이션과 마커는 3장 그대로 붙인다. |
| **reader** (QueryResults 데이터 소스, SQLite) | base의 결과 테이블(`query_<baseid>`)을 읽어서, 원래 하던 전 기간 집계와 `count_distinct`와 표시를 담당한다. base에 식별자(`usr_ifa` 등)를 남겨 두면 distinct를 SQLite에서 그대로 다시 셀 수 있어, HyperLogLog 같은 근사 없이 정확하다. |

결과적으로 매 실행의 원본 스캔이 전 기간에서 `refresh` 구간으로 줄고, reader는 이미 캐시된 좁은 행들만 SQLite로 집계한다.

### 5.3 주의사항

- **base는 행 수를 줄이지 않는다.** blob을 버려서 **행 하나의 바이트**만 줄일 뿐이고, distinct를 세려면 식별자를 남겨야 하므로 행 수는 원본과 같다. 시간당 수만 행이면 base도 수십만에서 백만 행이 된다. QueryResults(SQLite)는 이 정도를 잘림 없이 읽는다(실측 89만 행 왕복 정확 일치). base 결과 페이지는 렌더가 무겁지만 중간 산출물이라 사람이 열어 볼 일이 없다.
- **배열과 중첩 구조는 그대로 저장하지 말고, reader가 쓸 형태로 행 단위에서 미리 접는다.** `any_match(...)`는 bool로, `array_max(...)`는 스칼라로 만든다. bool은 `IF(...,1,0)` 정수로 저장해야 SQLite에서 안전하다. 문자열 `'false'`는 SQLite에서 참으로 평가된다.
- **reader는 SQLite 방언으로 써야 한다.**

  | Trino/Athena | SQLite |
  |---|---|
  | `format` | `printf` (`%,d` 천단위 구분 없음) |
  | `from_iso8601_timestamp(v) + date_add('day', N, ...)` | `datetime(replace(substr(v,1,19),'T',' '), '+N days')` |
  | `date_diff('day', a, b)` | `CAST(julianday(b) - julianday(a) AS INTEGER)` |

  날짜와 포맷 방언 차이 때문에 D-N 경계가 하루 어긋나거나 천단위 표기가 달라질 수 있지만, 판정 로직에는 영향이 없다.
- **base와 reader 둘 다 스케줄에 등록한다.** 증분은 스케줄 실행에서만 동작하기 때문이다. reader는 base의 최신 캐시를 읽으므로 둘의 실행 순서는 상관없다.
- **이 기법은 "원본 재스캔이 비쌀 뿐, 집계는 캐시된 행으로 재현 가능"할 때만 쓴다.** 롤링 윈도우로 대상을 선정하거나, 현재 상태를 전체 행에 퍼뜨려 거르는 필터는 행 단위 캐시로도 풀리지 않는다. 그건 [4장](#4-지금-상태를-참조하는-조인-처리하기)에서 다룬다.

### 5.4 실제 사례

`query 25890`(base, cvr 파싱 로그 행 단위) + `query 25745`(reader, 가동 및 안전 카드).

`raw_response` 6.5KB × 146만 행 / 48시간 ≈ 11GB를 매번 전부 스캔하던 것이, 파싱 base 약 90MB로 줄고 매시간 스캔 범위도 48시간에서 약 5시간으로 줄었다.

---

## 6. 함정 체크리스트

- **윈도우 함수의 partition 범위가 버킷 안에 갇히는가?** 하루 단위 윈도우라면 `bucket=day`가 필수다.
- **과거는 daily 테이블, 최근은 hourly 집계로 이어 붙이는 쿼리.** 증분에서는 daily 소스가 다시 읽히지 않으므로, 각 일자가 hourly 집계 결과로 확정되고 나중에 daily 값으로 교체되지 않는다. 두 테이블이 정보 손실 없이 같은 값으로 롤업된다는 전제를 판정에 명시한다. 그 전제가 성립하지 않으면 `refresh`를 두 소스가 갈리는 경계 너머로(예: `52h`) 키워서 daily로 한 번 다시 계산되게 한다.
- **마지막 `ORDER BY`.** 재사용한 과거 행 뒤에 새 행을 이어붙이는 방식이라 전체 정렬이 깨진다. 시각화가 자체적으로 정렬하면 문제없고, 아니라면 주의사항으로 명시한다.
- **`apply_auto_limit`.** matview 경로에서는 코드가 강제로 끈다. 지금까지 `LIMIT 1000`에 잘려서 보이던 쿼리는 전환 후 행 수가 늘어난다. 판정에 명시한다.
- **스케줄이 없는 쿼리.** 증분 경로를 타지 않는다. 절감을 받으려면 Redash 스케줄 등록이 먼저다.
- **하드코딩된 리스트나 `VALUES` 변경.** SQL 해시가 바뀌므로 자동으로 full 재적재가 걸린다. 따로 할 일이 없다.

---

## 7. 분석과 변환 결과를 적는 형식

**분석 요청에 답할 때**

- 결론(적합, 조건부, 부적합)
- 버킷(컬럼, 단위, 값 형식)
- 어노테이션 제안
- 마커를 심을 지점(바뀌는 줄만)
- "지금 상태"를 참조하는 조인에 대한 판정
- 예상 절감(현재 스캔 기간과 테이블 수 대비)
- 주의사항(스케줄 등록 여부, `apply_auto_limit`, 그 밖의 특이점)

**변환 요청에 답할 때**

- 어노테이션 1줄과 바뀌는 필터 줄만 낸다.
- 전체 SQL 재출력은 요청받았을 때만 한다.
